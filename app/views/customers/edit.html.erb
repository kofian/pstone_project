<h1>Editing customer</h1>

<%= render 'form' %>

<%= link_to 'Show', @customer, :class => "btn btn-default formButton"  %> |
<% if current_user.role = 'admin' %>
	<%= link_to 'Return to Dashboard', manage_customers_administrator_path, :class => "btn btn-default formButton"  %>
<% else %>
	<%= link_to 'Back to Home Page', root_path, :class => "btn btn-default formButton"  %>
<% end %>
