<%= form_for(@acct_type) do |f| %>
  <% if @acct_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acct_type.errors.count, "error") %> prohibited this acct_type from being saved:</h2>

      <ul>
      <% @acct_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
